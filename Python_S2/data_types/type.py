# ways to annotate a function to indicate input and output desired
def str_to_list(content: str) -> list[str]:
    return []

def call_functions(filtres: , articles:list[dict]) -> list[dict]:
    return [a for a in articles if allf(a) for f in filtres]
